#include <iostream>
using namespace std;

/**
 * NQ1-10: 简单乘积
 * 读取两个整数，输出 PROD = 乘积。
 * 时间: O(1) | 空间: O(1)
 */
int main() {
    int a, b;
    cin >> a >> b;
    cout << "PROD = " << a * b << endl;
    return 0;
}
