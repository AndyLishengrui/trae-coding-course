{
  "analysis": "## 解题思路：快速排序 (Quick Sort)\n\n快速排序是一种高效的排序算法，基于**分治法 (Divide and Conquer)** 的思想。它在平均情况下的时间复杂度为 $O(N \\log N)$，是实际应用中最常用的排序算法之一。\n\n### 1. 分治法的核心步骤\n\n对给定的数组 $A[l..r]$ 进行排序，快速排序包含三个核心步骤：\n\n1.  **选取基准 (Pivot Selection):** 从当前待排序的区间 $A[l..r]$ 中选择一个元素作为基准值 $x$（通常选择中间元素 $A[(l+r)/2]$）。\n2.  **分区操作 (Partitioning/Divide):** 重新排列数组，将所有小于等于 $x$ 的元素移到 $x$ 的左边，将所有大于等于 $x$ 的元素移到 $x$ 的右边。完成后，基准值 $x$ 位于一个确定的位置上，记为 $p$。\n3.  **递归排序 (Conquer):** 递归地对 $x$ 左侧的子区间 $A[l..p-1]$ 和右侧的子区间 $A[p+1..r]$ 进行快速排序。\n\n### 2. 分区操作的数学描述与实现（双指针法）\n\n分区操作是快速排序的关键。我们采用双指针法（Hoare Partition Scheme 的变体）来实现分区。\n\n给定区间 $[l, r]$，选取基准值 $x = A[(l+r)/2]$。\n\n设置两个指针：\n*   $i = l-1$（从左侧开始扫描）\n*   $j = r+1$（从右侧开始扫描）\n\n我们执行以下循环直到 $i \ge j$：\n\n1.  指针 $i$ 向右移动：`while (A[++i] < x);` \n    *   找到第一个大于等于基准值 $x$ 的元素。\n2.  指针 $j$ 向左移动：`while (A[--j] > x);` \n    *   找到第一个小于等于基准值 $x$ 的元素。\n3.  如果 $i < j$，则交换 $A[i]$ 和 $A[j]$。\n\n当循环结束时 ($i \ge j$)，数组被分成了两部分。由于我们使用的双指针策略，左侧子区间的右边界是 $j$，右侧子区间的左边界是 $j+1$（或者 $i$）。\n\n$$\n\\text{Left Subarray: } A[l..j] \quad (\\text{all elements} \\le x)\n$$\n$$\n\\text{Right Subarray: } A[j+1..r] \quad (\\text{all elements} \\ge x)\n$$\n\n### 3. 递归关系与时间复杂度\n\n快速排序的递归关系可以表示为：\n\n$$T(N) = T(k) + T(N-k-1) + O(N)$$\n\n其中 $k$ 是左子数组的大小，$O(N)$ 是分区操作的时间开销。\n\n*   **最佳情况 (理想平衡)：** 如果每次分区都能将数组完美地分成大小相等的两半，即 $k \approx N/2$。\n    $$T(N) = 2T(N/2) + O(N)$$\n    此时，时间复杂度为 $O(N \\log N)$。\n*   **最坏情况 (极端不平衡)：** 如果每次分区都选中了最大或最小的元素作为基准，导致 $k=0$ 或 $k=N-1$。\n    $$T(N) = T(N-1) + O(N)$$\n    此时，时间复杂度退化为 $O(N^2)$。\n\n但在实际应用中，由于基准值选择的多样性，快速排序的平均性能极高。",
  "code": "#include <iostream>\n#include <vector>\n#include <algorithm>\n#include <cmath>\n\n// 使用Google C++ Style Guide的命名规范，QuickSort作为全局函数。\n// 对数组arr的 [l, r] 区间进行快速排序。\nvoid QuickSort(std::vector<int>& arr, int l, int r) {\n  // 1. 递归终止条件：当区间长度为0或1时，已经有序\n  if (l >= r) {\n    return;\n  }\n\n  // 2. 选择基准值 (Pivot Selection)\n  // 选择中间元素作为基准值，这是CP中常见的选择方式。\n  int pivot = arr[(l + r) / 2];\n\n  // 初始化双指针。i从左侧l-1开始，j从右侧r+1开始。\n  int i = l - 1;\n  int j = r + 1;\n\n  // 3. 分区操作 (Partitioning)\n  while (i < j) {\n    // i 向右移动，找到第一个大于等于 pivot 的元素\n    // 注意：必须使用 do-while 结构确保 i 和 j 至少移动一次\n    do {\n      i++;\n    } while (arr[i] < pivot);\n\n    // j 向左移动，找到第一个小于等于 pivot 的元素\n    do {\n      j--;\n    } while (arr[j] > pivot);\n\n    // 如果 i 仍在 j 的左边，说明找到了需要交换的元素对\n    if (i < j) {\n      std::swap(arr[i], arr[j]);\n    }\n  }\n\n  // 4. 递归调用 (Conquer)\n  // 经过分区，j 指针将数组分成了 [l, j] 和 [j+1, r] 两个子区间。\n  // 递归排序左半部分（所有元素 <= pivot 的部分）\n  QuickSort(arr, l, j);\n\n  // 递归排序右半部分（所有元素 >= pivot 的部分）\n  QuickSort(arr, j + 1, r);\n}\n\nint main() {\n  // 提升输入/输出效率\n  std::ios_base::sync_with_stdio(false);\n  std::cin.tie(NULL);\n\n  int n;\n  if (!(std::cin >> n)) return 0;\n\n  std::vector<int> arr(n);\n  for (int i = 0; i < n; ++i) {\n    if (!(std::cin >> arr[i])) return 0;\n  }\n\n  // 调用快速排序函数\n  if (n > 0) {\n    QuickSort(arr, 0, n - 1);\n  }\n\n  // 输出排好序的数列\n  for (int i = 0; i < n; ++i) {\n    std::cout << arr[i] << (i == n - 1 ? \"\" : \" \");\n  }\n  std::cout << \"\\n\";\n\n  return 0;\n}\n"
}