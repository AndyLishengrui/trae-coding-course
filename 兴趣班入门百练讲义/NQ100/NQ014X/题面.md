# 1.14 数组的左下半部分 NQ014X

## 描述
输入一个二维数组M[12][12]，根据输入的要求，求出二维数组的左下半部分元素的平均值或元素的和。

## 输入
第一行输入一个大写字母，若为S，则表示需要求左下半部分的元素的和；若为M，则表示需要求左下半部分的元素的平均值。
接下来12行，每行包含12个用空格隔开的小数，表示这个二维数组，其中第i+1行的第j+1个数表示数组元素M[i][j]。
数据范围：-100.0 ≤ M[i][j] ≤ 100.0

## 输出
输出一个数，表示所求的平均数或元素的和的值，保留一位小数。

## 输入样例
```
S
8.7 5.6 -2.0 -2.1 -7.9 -9.0 -6.4 1.7 2.9 -2.3 -8.4 0.0
-7.3 -2.1 0.6 -9.8 9.6 5.6 -1.3 -3.8 -9.3 -8.0 -2.0 2.9
-4.9 -1.5 -5.5 -5.2 -4.5 -4.1 7.6 6.9 -9.0 6.8 9.1 -8.5
-2.5 -2.0 -1.8 -5.9 -7.2 -3.3 -1.8 -4.3 -5.5 -7.1 -7.7 -3.7
-7.0 -1.2 7.8 7.1 -3.7 7.8 -2.3 4.3 0.2 -4.6 -6.6 -4.7
-3.2 -3.4 -4.7 4.7 3.6 8.8 5.1 -3.2 -1.9 2.8 -4.3 -1.4
-3.0 -2.0 -7.1 -1.3 -3.7 -1.3 -1.4 -3.4 -4.0 -2.7 -4.4 -3.4
-2.5 -0.9 9.2 -8.4 -9.4 -5.1 6.5 1.5 3.4 -0.1 -7.0 -6.8
-1.8 -4.9 2.2 -8.7 -9.4 4.8 2.3 0.2 -3.2 -7.5 -4.0 -9.9
-1.1 -2.9 0.7 -0.7 -7.4 8.6 -2.0 -1.7 -0.7 -0.8 -5.4 -3.8
-5.4 -8.7 -7.1 -5.7 -1.4 -1.8 -3.3 -4.1 -2.7 -2.9 -4.6 -5.5
-2.8 -9.1 7.1 -0.2 -8.6 -6.5 1.1 -0.1 -3.6 4.0 -5.4 1.1
```

## 输出样例
```
-2.8
```

## 解题思路
本题要求根据输入的操作类型，计算二维数组左下半部分的和或平均值。左下半部分是指主对角线以下的部分，即j < i的元素。首先读取操作类型（S表示求和，M表示求平均值），然后读取12x12的二维数组，计算左下半部分的和和元素个数，最后根据操作类型输出和或平均值，保留一位小数。

## 参考代码(C++)
```cpp
// NQ014X 数组的左下半部分
#include <iostream>
#include <iomanip>
using namespace std;

int main() {
    double a[12][12];
    char t;
    cin >> t;
    for(int i=0;i<12;++i) {
        for(int j=0;j<12;++j) {
            cin >> a[i][j];
        }
    }
    double s = 0;
    int cnt = 0;
    for(int i=0;i<12;++i) {
        for(int j=0;j<i;++j) {
            s += a[i][j];
            cnt++;
        }
    }
    cout << fixed << setprecision(1);
    if(t == 'S') cout << s << endl;
    else cout << s/cnt << endl;
    return 0;
}
```

## 参考代码(Python)
```python
# NQ014X 数组的左下半部分
t = input().strip()
a = []
for _ in range(12):
    row = list(map(float, input().split()))
    a.append(row)
s = 0
cnt = 0
for i in range(12):
    for j in range(i):
        s += a[i][j]
        cnt += 1
if t == 'S':
    print(f"{s:.1f}")
else:
    print(f"{s/cnt:.1f}")
```