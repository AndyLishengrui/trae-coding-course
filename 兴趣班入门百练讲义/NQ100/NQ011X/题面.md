# 1.11 数组的左上半部分 NQ011X

## 描述
输入一个二维数组M[12][12]，根据输入的要求，求出二维数组的左上半部分元素的平均值或元素的和。

## 输入
第一行输入一个大写字母，若为S，则表示需要求左上半部分的元素的和；若为M，则表示需要求左上半部分的元素的平均值。
接下来12行，每行包含12个用空格隔开的小数，表示这个二维数组，其中第i+1行的第j+1个数表示数组元素M[i][j]。
数据范围：-100.0 ≤ M[i][j] ≤ 100.0

## 输出
输出一个数，表示所求的平均数或元素的和的值，保留一位小数。

## 输入样例
```
M
-0.4 -7.7 8.8 1.9 -9.1 -8.8 4.4 -8.0 0.5 -5.8 1.3 -8.0
-1.7 -4.6 -7.0 4.7 9.6 2.0 8.2 -6.4 2.2 2.3 7.3 -0.4
-8.1 4.0 -6.9 8.1 6.2 2.5 -8.2 -6.2 -1.5 9.4 -9.8 -3.5
-3.4 -2.4 -8.0 0.7 -2.9 5.4 -8.4 -5.4 -5.1 -6.0 -8.5 8.1
6.2 -1.5 -6.9 3.1 -7.9 5.1 -8.8 0.9 -7.4 -3.9 -2.7 0.9
-6.8 -0.8 -9.9 9.1 -3.7 -8.4 4.4 9.8 -6.3 -6.4 -3.7 2.8
-3.8 -5.4 -4.6 2.0 4.0 9.2 -8.0 0.5 -3.9 6.5 -4.3 -9.9
-7.2 -6.2 -1.2 4.1 -7.4 -4.6 4.7 -0.4 -2.2 -9.1 0.4 -5.8
-8.0 -2.0 -5.3 -3.7 -2.2 -3.9 -8.6 -7.0 -4.1 -3.2 -5.9 -4.2
-1.2 -6.2 -8.2 7.0 -5.3 4.4 9.5 7.2 2.3 4.4 3.2 -2.0
-6.9 -6.2 5.1 8.5 7.1 -0.8 -0.7 2.7 -6.0 4.2 -8.2 -9.8
-3.5 -1.7 5.4 2.8 1.6 -1.0 -6.1 7.7 -6.5 -8.3 -8.5 9.4
```

## 输出样例
```
-0.8
```

## 解题思路
本题要求根据输入的操作类型，计算二维数组左上半部分的和或平均值。左上半部分是指次对角线以上的部分，即j < 11 - i的元素。首先读取操作类型（S表示求和，M表示求平均值），然后读取12x12的二维数组，计算左上半部分的和和元素个数，最后根据操作类型输出和或平均值，保留一位小数。

## 参考代码(C++)
```cpp
// NQ011X 数组的左上半部分
#include <iostream>
#include <iomanip>
using namespace std;

int main() {
    double a[12][12];
    char t;
    cin >> t;
    for(int i=0;i<12;++i) {
        for(int j=0;j<12;++j) {
            cin >> a[i][j];
        }
    }
    double s = 0;
    int cnt = 0;
    for(int i=0;i<12;++i) {
        for(int j=0;j<11-i;++j) {
            s += a[i][j];
            cnt++;
        }
    }
    cout << fixed << setprecision(1);
    if(t == 'S') cout << s << endl;
    else cout << s/cnt << endl;
    return 0;
}
```

## 参考代码(Python)
```python
# NQ011X 数组的左上半部分
t = input().strip()
a = []
for _ in range(12):
    row = list(map(float, input().split()))
    a.append(row)
s = 0
cnt = 0
for i in range(12):
    for j in range(11 - i):
        s += a[i][j]
        cnt += 1
if t == 'S':
    print(f"{s:.1f}")
else:
    print(f"{s/cnt:.1f}")
```