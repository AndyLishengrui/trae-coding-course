# JD083：剑气归正

> **AcWing 810** | 第7章 · 以招创招 | 知识点：函数

## 题目描述

李少白在练剑气外放时，需要计算剑气落点与自己的距离。距离本该是正数，但有时算出的结果是负数——因为方向搞反了。

赵晴儿说："别每次遇到负数就手动取反，写一个函数，不管传入正数还是负数，都返回它的绝对值。"

## 输入格式

一个整数x。

## 输出格式

输出x的绝对值。

## 样例

输入：

```text
-5
```

输出：

```text
5
```

## 解题思路

if x < 0: return -x, else: return x。
